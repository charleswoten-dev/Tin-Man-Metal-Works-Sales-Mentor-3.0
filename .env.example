# ── Server-side secrets (live in server/.env, never committed) ──
# Get a fresh key at https://console.anthropic.com  → API Keys
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ANTHROPIC_MODEL=claude-sonnet-4-6

# Supabase (project URL + anon key — anon key is safe for client use)
SUPABASE_URL=https://retufxuvstfupekimvhk.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Server
PORT=3001

# ── Client (Vite) — these get bundled into the browser, so only PUBLIC values ──
# Lives in client/.env, prefix must be VITE_
VITE_SUPABASE_URL=https://retufxuvstfupekimvhk.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
VITE_API_BASE=http://localhost:3001
